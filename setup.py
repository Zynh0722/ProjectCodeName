from setuptools import setup
from setuptools import find_packages

setup(name='ProjectCodeName',
      version='0.1',
      description='',
      author='Nicholas Ludwig, Ethan Hansen, Kaylani Kam, Nathan Gabrielsen',
      packages=find_packages(),
      zip_safe=False)